import 'package:flutter/material.dart';

class DisplayDevangsPrivacyNoticesView extends StatelessWidget {
  const DisplayDevangsPrivacyNoticesView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        centerTitle: true,
        title: const Text("Devang's Privacy Notices"),
      ),
    );
  }
}
